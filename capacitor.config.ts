import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.polvoDaSorte',
  appName: 'Polvo da Sorte',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
